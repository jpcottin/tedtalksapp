# Release signing configuration — TEMPLATE.
#
# Copy to `keystore.properties` (which is gitignored) and fill in real values,
# OR provide the same values via environment variables in CI:
#   storeFile     -> RELEASE_STORE_FILE
#   storePassword -> RELEASE_STORE_PASSWORD
#   keyAlias      -> RELEASE_KEY_ALIAS
#   keyPassword   -> RELEASE_KEY_PASSWORD
#
# Paths are resolved relative to the repository root.
# If none of these are set, the release build falls back to debug signing.

storeFile=keystore/release.jks
storePassword=CHANGE_ME
keyAlias=tedtalks
keyPassword=CHANGE_ME
